public class Child extends Parent{
    public static void main(String args[]) {
        Child obj=new Child();

        obj.Greet();
    }
}


// This is the child class which will inherit the parent class and use its methods in itself.

// extends stands for that the Parent class of Parent.java file is used or called in this file.

// An object of Child class is declared in it's own main method.

// The object of child is called followed by a period followed by the Method name of parent class which is needed.